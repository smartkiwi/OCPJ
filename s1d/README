1.d. Create a class that uses static initialization blocks, instance initialization blocks and a constructor, and prove the sequence in which they get called when the class is invoked and a new object is created.

Time: 10 minutes 
+10 minutes procrastinating in frf